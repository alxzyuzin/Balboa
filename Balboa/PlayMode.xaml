<UserControl
    x:Class="Balboa.PlayMode"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Balboa"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="250" Height="250">

    <Grid Margin="20,0,0,0">
        <Grid.Resources>
            <Style x:Name="ToggleSwitchStyle" TargetType="ToggleSwitch">
                <Setter Property="Width" Value="200"/>
                <Setter Property="Height" Value="80"/>
                <Setter Property="OffContent" Value="Off"/>
                <Setter Property="OnContent" Value="On"/>
                <Setter Property="FontSize" Value="18"/>
            </Style>

        </Grid.Resources>
        <StackPanel>
            <ToggleSwitch Header="Random"  IsOn="{Binding Random}"  Style="{StaticResource ToggleSwitchStyle}"  Toggled="ts_Random_Toggled"/>
            <ToggleSwitch Header="Repeat"  IsOn="{Binding Repeat}"  Style="{StaticResource ToggleSwitchStyle}"  Toggled="ts_Repeat_Toggled"/>
            <ToggleSwitch Header="Consume" IsOn="{Binding Consume}" Style="{StaticResource ToggleSwitchStyle}"  Toggled="ts_Consume_Toggled"/>
        </StackPanel>
    </Grid>
</UserControl>
